services:
  - type: web
    name: balanceshift
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --config gunicorn.conf.py --timeout 180
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
